#include "main.h"


/**
 * _abs - computes the absolute value
 * n: int to compute as absolute
 * Return: the absolute value
 **/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

